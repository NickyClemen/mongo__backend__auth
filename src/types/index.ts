export type VerifyToken = {
    name:string,
    message:string,
    success:boolean,
}

export type Payload = {
    username:string,
    email:string,
    name:string,
    lastname:string,
    age:number,
}